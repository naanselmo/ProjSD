uddi.enabled=true

uddi.url=${uddi.url}
ws.name=${ws.name}

# ws url below is used only if uddi is disabled above
ws.url=${ws.url}

# Properties for supplier tests
suppliers.count=${suppliers.count}
suppliers.name.prefix=${suppliers.name.prefix}
suppliers.uddi.url=${suppliers.uddi.url}
